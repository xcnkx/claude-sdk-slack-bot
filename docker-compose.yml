services:
  bot:
    build: .
    env_file: .env
    # CLAUDE_CODE_OAUTH_TOKEN is injected via `make docker-run`
    volumes:
      - ~/.claude/skills:/home/bot/.claude/skills:ro
      - ~/Library/Application Support/gogcli:/home/bot/.config/gogcli:ro
