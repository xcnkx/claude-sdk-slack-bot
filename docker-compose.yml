services:
  bot:
    build:
      context: .
      secrets:
        - github_token
    env_file: .env
    # Tokens are injected via `make docker-run`

secrets:
  github_token:
    environment: GITHUB_TOKEN
